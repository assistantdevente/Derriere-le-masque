// Recette pour envoyer un mail via Mailjet
function envoyerAnalyse(emailClient, prenom) {
    const api_key = 'e40c5cb9d7ecaedda3b91ad029e415b4';
    const api_secret = 'febd9df468934e46cff0fd7851b464cc';

    console.log("Envoi de l'analyse à " + emailClient);
    
    // Ici, on dira plus tard à l'IA de générer les 15 paragraphes
    alert("Le facteur Mailjet prépare vos 15 paragraphes pour " + emailClient);
}
